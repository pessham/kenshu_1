
# AI活用研修 第4回: ローカルLLM構築 〜「社外秘」を扱える最強の専用環境を作る〜

## セミナー台本（120分想定）

## 【事前準備】（開始前）

- **確認事項**:
    
    - 全員のPCに `LM Studio` がインストールされているか。
        
    - 宿題のモデル `Llama 3.1 8B Instruct (Q4_K_M)` がダウンロード済みか。
        
    - 宿題の「秘伝のタレ（Markdown化した社外秘資料）」が手元にあるか。
        
- **環境**:
    
    - A氏: VS Code (Python / Semantic Kernel)
        
    - B氏: Visual Studio (VB.NET / Semantic Kernel)
        

---

## スライド1: タイトル & 本日のゴール（5分）

(JSON: type: "title")

### 台本

「皆さん、こんにちは。第4回です。いよいよ**『インターネットの線を抜く』**時が来ました。

システム開発会社で最も多い悩みの一つが**「仕様書がない（または古い）塩漬けコード」**の保守です。ソースコードは絶対に社外に出せないため、ローカルLLMの独壇場です。

- 例えば担当者が辞めた5年前のVB.NET/PHPコードがあり、仕様がブラックボックス化している。
- 過去のエラーログ。過去に誰かが解決しているはずなのに、WikiやSlack、ファイルサーバーに散らばっていて見つからない

という課題があったとします。

社外秘のデータをChatGPT（OpenAIのクラウド）に投げるわけにはいきません。

これらのコードやエラーログ、Gitの「Commitメッセージ」等をローカルに置いてSemantic Kernelにコードを1ファイルずつ読み込ませます。

そしてローカルLLMがコードを解析し、「日本語の仕様説明」を生成。

さらに、「単体のテストコード」を生成。して仕様書（Markdown）とテストコード（.vb / .php）をファイル保存。といったことが可能になります。


本日のゴールはただ一つ。


**『完全にオフラインのPCの中で、ChatGPT並みのAIを動かし、社外秘データを食わせる』**ことです。


---

## スライド2: ローカルLLMの仕組み（10分）

(JSON: type: "analogy")

### 台本

「作業に入る前に、今PCの中に入っているツールについて整理します。宿題で『LM Studio』と『Llamaモデル』を入れてもらいましたね。

これを音楽に例えるとわかりやすいです。

- **LM Studio** = **『CDプレイヤー（再生機）』**
    
    - AIを動かすためのソフトです。これ自体に知能はありません。
        
- **Llama 3.1 8B (モデルファイル)** = **『CD（音楽データ）』**
    
    - これが『脳みそ』です。Meta社が作った優秀な脳です。
        
    - 今回選んだ『Q4_K_M』というのは、mp3圧縮のようなものです。量子化という技術を使って、大きな数字を小さくて扱いやすい数字に置き換えることで、モデルのサイズを小さくし、LLMのサイズを圧縮しています。Mがミディアムのことで、だいたい中程度に圧縮しているということです。少し画質（知能）を落として、皆さんのノートPCのメモリに乗るサイズ（約5〜8GB）に圧縮してあります。
        

今日は、この『CD』を『プレイヤー』に入れて再生し、そこに前回作った『歌詞カード（RAGデータ）』を渡して歌わせるイメージです。」

Llama 3.1とGPTossの違い

より一般的な感覚のあるGPT OSSを使わずに今回Llama 3.1を使用する理由としては、Llama 3.1の方がファインチューニングが可能だからです。基本的にはラグに対してデータを読み込ませるだけで対応することが可能だと思いますが、LLMの考え方のそのものを変更する、追加学習するということが、Llama 3.1だと可能になります。

また、完全ローカルオンプレミス環境で使用するのであれば、GPT-OSSよりもLlama 3.1の方が使い勝手が良いものになると言われています。

### 【要約】Llama 3.1のポイント

- **最強のオープンモデル：** 405B（4050億パラメータ）モデルは、GPT-4oやClaude 3.5 Sonnetに匹敵する最高峰の性能。
    
- **柔軟なラインナップ：** 8B、70B、405Bの3サイズ展開で、用途やハードウェアに合わせた選択が可能。**8B（軽量）：** 高速レスポンス。エッジデバイスや個人PC向け。GPUメモリは標準で約16GB,軽量化したモデルで約4〜6GB
    
- **商用利用可能：** 月間アクティブユーザー7億人以下なら無償で商用利用が可能。
    

---

## スライド3: ハンズオン1 - LM Studioの起動とサーバー化（20分）

**（修正コンセプト: 「自分のPC内に、ゼロからAIサーバーを立ち上げる」）**

(JSON: type: "step")

--- 
でもを見せる

semantic-kernelのターミナルでの作業をもう一回再起動させる

ollama run llama3.1

source venv/bin/activate

python3 main.py


例文：
T_ORDERテーブルのDEL_FLGが1のデータを削除していい？

期待される回答：

もし普通のChatGPTなら「はい、削除フラグなら通常は削除して良いですが、バックアップを…」などと答えます。 しかし、あなたのAIは**マニュアル通りにこう答えるはず**です：

> **「待て！ DEL_FLGは削除フラグではなく『配送済みフラグ』だ！ DELETEという文字がMEMO欄にあるのが削除データだ。絶対に触るな！」**

例文：
顧客サーバーAで502エラーが出た。再起動して復旧させます。

期待される回答

> **「再起動は絶対にするな！ DBチェックで朝まで復旧しなくなるぞ。まず『DefOneAgent.exe』をタスクキルして、共有メモリを解放しろ。」**

--- 
### 台本

「では、実際に構築に入りましょう。これまでの研修ではVSコードからオンラインでアクセスできるGitHub CopilotなどのAIにアクセスしていましたが、今日は**皆さんのPCの中に、皆さん専用のサーバー**を立ち上げます。

LM Studioを起動してください。

**【Step 1: 脳みその動作確認】**

1. 画面上の虫眼鏡アイコンから、ダウンロード済みの `Llama 3.1` を選択してください。（画面上部の緑のバーになります）
    
2. チャット画面で『こんにちは、あなたは誰ですか？』と打ってみてください。
    
3. **（重要）ここでLANケーブルを抜いて（Wi-Fiを切って）ください。**
    

…どうですか？ 返事が来ましたね？ これが『ローカルLLM』です。今、通信は一切発生していません。世界中の誰もこの会話を盗み見ることは不可能です。

**【Step 2: サーバーとして起動する】**

次に、このAIをプログラムから使えるように『サーバー化』します。 左メニューの『`<->` (Developer)』アイコンを押してください。

ここが今日のハイライトです。

- `Start Server` ボタンを押してください。
    
- ログに `Running on http://localhost:1234` と出たら成功です。
    

おめでとうございます。これで皆さんのPCは**『小さなOpenAI APIサーバー』**になりました。 ここからは、この『localhost:1234』という住所に向かってプログラムを書くことになります。」

---

## スライド4: ハンズオン2 - Semantic Kernelでの接続（コード実装）（30分）

**（修正コンセプト: 「最初からローカルホストに向けてコードを書く」）**

(JSON: type: "codeDiff")

### 台本

「箱（サーバー）はできました。次はそこにつなぐ『手足（プログラム）』を作ります。 ここでSemantic Kernelの凄さが分かります。

配布したフォルダ（`Handson_Materials`）の中に、私が用意した『ほぼ完成済みのコード』が入っています。

これを開いて、先ほどの『住所』を教えるだけでつながるように作ってあります。

**【B氏（VB.NET）への指示】**

Visual Studioで、配布された `Program.vb` を開いてください。 12行目あたりを見てみましょう。

この `endpoint` の `""` の中に、先ほどコピーしたURL（`http://localhost:1234/v1`）を貼り付けてください。

**【A氏（Python）への指示】**

Aさんは VS Code で `main.py` を開いてください。 こちらも冒頭（10行目あたり）に設定エリアがあります。

同じく、ここにURLを貼り付けて保存してください。」

**【実行確認】**

では、実行してみましょう。 プログラムから『自己紹介して』と投げて、**LM Studioのログ画面（緑色の文字が流れる画面）**を見てください。

…リクエストが飛んできて、ローカル側で処理が走りましたね？ これで、**『プログラムから、自分のPC内のAIを操作する』**環境が完成しました。」

### 講師の補足

「Bさん、ここがポイントです。 今回はローカルにつなぎましたが、もし将来『やっぱりGPT-4を使いたい』となったらどうすればいいでしょう？

そうです。ここの `endpoint` を消して、`apiKey` を本物に書き換えるだけです。 **『接続先の設定を変えるだけで、脳みそを自由にすげ替えられる』**。 これが、直接通信を書かずに Semantic Kernel を使う最大のメリットなんです。」


「接続先が変わったところで、少し立ち止まりましょう。 なぜ、我々は生のAPIを叩かずに、わざわざ『Semantic Kernel』という面倒なクッションを挟むのでしょうか？

それは、**AIに『皆さんの既存コード』という武器を持たせるため**です。

SKでは、AIへの指示を『スキル（プラグイン）』という単位で管理します。これには2種類あります。

1. **Semantic Function（文系スキル）**:
    
    - 『要約して』『翻訳して』といった、プロンプトで書かれた指示。
        
2. **Native Function（理系スキル）**:
    
    - **ここが重要です。** C#やVB.NETで書かれた、**普通のメソッド**です。
        

例えば、Bさんが昔作った『複雑な給与計算ロジック（VB.NET）』があるとします。 これをSKに『Native Function』として登録しておくと、AIは会話の中で必要だと判断した瞬間に、**自らそのVB.NETの関数を実行して、計算結果を取り込みます。**

**【Bさんへのメッセージ】** つまり、SKを使うということは、**『あなたの過去の資産（VB.NETコード）が、そのままAIの手足になる』**ということです。 単にチャットするだけでなく、AIが『計算』し、『DBを検索』し、『ファイルを保存』する。その実動部隊をVB.NETで書けるのがSKの強みです。」

---

## スライド5: ハンズオン3 - 「秘伝のタレ」RAGの実装（40分）

(JSON: type: "process")

### 台本

「箱（サーバー）と手足（接続）は完成しました。 いよいよ**『脳みそに記憶（社外秘ナレッジ）』**を焼き付けます。

ここが本日のクライマックスです。 Aさんが整理してくれた、『あの時どうやって解決したか』という泥臭い障害対応ログや、『ベテランしか知らない仕様』をAIに読ませます。

これができれば、**深夜3時にベテランの電話を鳴らして怒られること**はもうなくなります。」


**【シナリオ：魔の502エラー】**

- **状況**: 深夜、顧客サーバーで「502 Bad Gateway」が発生。
    
- **新人の行動**: 「とりあえず再起動すれば直るかな？」と思っている。
    
- **社外秘の事実**: 実はウイルス対策ソフトが原因。**再起動するとDB整合性チェックが走り、朝まで復旧しなくなる（＝大事故）。**
    

**【A氏・B氏への指示】**

「お手元のコード（`main.py` または `Program.vb`）の『プロンプト作成エリア』を見てください。 現在は空欄、もしくは仮の文章が入っていると思います。

ここを、**『システム開発会社のベテランエンジニア』**の人格に書き換えます。 以下のテンプレートを使ってください。」

Plaintext

```
あなたは工場の熟練アシスタントです。
以下の【社内マニュアル】のみに基づいて回答してください。
知識にない場合は「わかりません」と答えてください。

【社内マニュアル】
{{$manual_content}}  <-- ここにファイルの中身を流し込む

【ユーザーの質問】
{{$user_input}}
```

「書き換えたら実行してください。 そして、こう質問してみてください。

**『サーバーAで502エラーが出た。再起動して復旧させます。』**

…どう返ってきましたか？ 普通のChatGPTなら『はい、再起動は一般的な対処法です』と答えるでしょう。

しかし、皆さんのローカルAIはこう答えたはずです。 **『待て！再起動は絶対にするな！ DefOneAgentをタスクキルしろ！』**

…これがRAGの威力です。 ネットの一般論ではなく、**『御社のシステムを守るための、御社だけの正解』**を、AIが叩き返してくれました。」

---

## スライド6: ローカルLLMの「弱点」とチューニング（10分）

(JSON: type: "compare")

### 台本

「感動的な瞬間ですが、冷静な目も持ちましょう。GPT-4と比べてどう感じましたか？

- **速度**: 多分、少し遅いです（PCのスペックに依存）。
    
- **精度**: たまに日本語が怪しかったり、指示を無視したりしませんか？
    

これが『8B（80億パラメータ）』の限界です。GPT-4はこの100倍以上のサイズがあります。

だからこそ、第1回で学んだ**『プロンプトエンジニアリング』**が重要になります。

- 曖昧な指示は通じません。
    
- 『JSONで返して』という指示も、例（Few-shot）をつけないと失敗しやすいです。
    

**Aさんの今後の役割**は、この『小さな脳みそでも動くようにプロンプトを最適化する』こと、あるいは『もっと賢いモデル（コマンドR+など）を探してくる』ことです。

**Bさんの役割**は、『AIが間違ったフォーマットで返してきた時に、プログラム側でエラーハンドリングする（再試行させる）』仕組みを強固にすることです。」

---

## Slide 7 - ペアワーク「最終課題：業務改善プロトタイプ策定」（30分）

(JSON: type: "workshop")

### 目的

ただの「雑談」で終わらせないよう、**「①課題、②解決策（AI）、③実装構成」**の3点をMarkdownファイルに落とし込むことをゴールにします。

### 台本

「さて、技術的な武器は揃いました。しかし、使わなければ意味がありません。 残りの時間を使って、次回（最終回）に向けた**『作戦会議』**を行っていただきます。

Aさん（データ・AI担当）とBさん（アプリ実装担当）、ペアになって以下のテーマを決定してください。

**【議論すべき3つのポイント】**

1. **『何の業務を』変えるか？（Pain Point）**
    
    - 例：「毎日届く大量のエラーメールを見るのが辛い」
        
    - 例：「ベテランしか知らない『あの仕様』を若手が毎回聞きに来て作業が止まる」
        
2. **『どのAI』で解決するか？（Solution）**
    
    - 今日学んだ**ローカルLLM（Llama 3）**を使うべきか？（社外秘データを使う場合）
        
    - それとも、精度優先で**Azure OpenAI（GPT-4）**を使うべきか？（公開データで良い場合）
        
    - ※今日はローカルLLMの研修ですが、実務での成果を優先して使い分けてOKです。
        
3. **『どんな形』で提供するか？（UI/UX）**
    
    - Bさんの出番です。VB.NETの画面か？ 既存システムのボタンに追加するか？ Webアプリか？
        

**【講師からのアドバイス】** 『壮大なDX』を目指さないでください。 **『半径5メートルの、具体的で泥臭い困りごと』**の方が、AI導入は成功しやすいです。 特にローカルLLMは『狭く深い知識（今日のマニュアルのような）』が得意です。

では、今から30分取ります。お手元のVS Codeで『Project_Plan.md』というファイルを作り、2人で埋めていってください。」

---

### 配布資料：ワークショップ用テンプレート

（これを画面に映すか、テキストで配布します）

Markdown

```
# 最終課題：業務改善プロトタイプ企画書

## 1. 解決したい課題（Pain）
- **現状**: （例：毎日17時に、日報をExcelから基幹システムに手入力している）
- **困りごと**: （例：コピペミスが多い、単純作業で30分取られる）

## 2. AIソリューション（Brain）
- **使用モデル**: （Llama 3.1 Local / GPT-4 Cloud）
- **食わせるデータ(RAG)**: （例：過去3年分のトラブル対応履歴.csv）
- **AIへの指示**: （例：エラーログを読み込み、過去の類似事例トップ3と解決策を提示せよ）

## 3. アプリケーション構成（Body）
- **入力インターフェース**: （例：VB.NETで作った専用フォーム）
- **出力形式**: （例：画面に解決策を表示 ＋ クリップボードにコピー）
- **担当割り当て**:
    - Aさん：プロンプトの調整、過去データのクリーニング
    - Bさん：Semantic Kernelを使ったVB.NETアプリの実装
```

---

## 修正版 Slide 8: まとめと最終回に向けて（5分）

(JSON: type: "checklist")

### 台本

「お疲れ様でした。各ペア、熱い議論ができていましたね。 『ゴミ箱の中のエラーログを宝の山に変える』や『属人化したベテランの知恵をデジタル化する』など、面白そうなテーマが聞こえてきました。

**【次回のゴール】** 今日作ったその企画書（Markdown）を、**『動くモノ』**にして持ってきてください。


- **持ち時間**: Aさん・Bさんそれぞれで**合計30分**（発表20分 ＋ 質疑応答10分）。

- **形式**: デモ（実演）を必ず含めること。
    
- **必須条件**: 発表資料（スライド）の作成にAIを使用し、**『どういうプロンプトで資料を作ったか』**も最後にネタ晴らしすること。」

- **完成度**: 100%でなくて構いません。エラーが出てもいいです。
    
- **重要視すること**: 『実際に業務データを入れて動かしてみた結果、何が起きたか』という**リアリティ**です。
    

もし実装で詰まったら、今日の講義で使った `main.py` や、LM Studioのログを見返してください。答えはそこにあります。

それでは、第4回研修を終了します。次回、皆さんのプロトタイプが見られることを楽しみにしています！」

---


【補足】まとめて一気に作る方法


---

## 3. 「秘伝のタレ」アプリの作成

Windowsのデスクトップ等に作業用フォルダ（例: `MyAI`）を作ります。 その中に、以下の2つのファイルを作成してください。（メモ帳で作成し、拡張子を変更すればOK）

### ファイル①：`secret_manual.md` （秘伝のタレ本体）

Markdown

```
# 社内システム開発・運用ナレッジ（社外秘）

## 1. トラブルシューティング: 顧客サーバーA (AWS)
- **事象**: HTTP 502 Bad Gateway が深夜に多発する。
- **真の原因**: 
    - ログにはメモリ不足と出るが、本当の原因はウイルス対策ソフト「DefenseOne」の競合。
    - 特定のTempファイルをロックしてしまい、IISがタイムアウトしている。
- **対処法（再起動厳禁）**:
    1. サーバー再起動は**絶対にしてはならない**（DBの整合性チェックが走り、朝まで復旧しなくなる）。
    2. まずタスクキルで `DefOneAgent.exe` を落とす。
    3. `C:\App\Unlocker.bat` を管理者権限で実行し、共有メモリを解放する。
    4. これで502は自然解消する。

## 2. レガシーコード解説: 生産管理システム(旧)
- **対象**: `OrderManager.dll` / `T_ORDER` テーブル
- **隠された仕様**: 
    - カラム `DEL_FLG` は削除フラグではない。実は「配送(Delivery)済みフラグ」。
    - **削除済みデータ**は、`MEMO` 欄に「DELETE」という文字列が入っているレコードを指す。
    - **警告**: `WHERE DEL_FLG = 1` で削除データを検索しようとすると、配送済みデータがヒットして請求ミスになる。絶対に触るな。
```

---


### ファイル②：`main.py` （AIを動かすプログラム）

**ファイル名**: `main.py`

Python

```
import asyncio
import os
from semantic_kernel import Kernel
from semantic_kernel.connectors.ai.open_ai import OpenAIChatCompletion
from semantic_kernel.functions import KernelArguments

# ==========================================
# 【ハンズオン1】ここを設定する
# ==========================================
# TODO: LM StudioのローカルサーバーのURLを入れる
ENDPOINT = ""  
MODEL_ID = "llama-3.1-8b-instruct"
# ==========================================

async def main():
    # 1. カーネルの初期化
    kernel = Kernel()

    # 2. AIサービスの登録
    service = OpenAIChatCompletion(
        ai_model_id=MODEL_ID,
        api_key="not-needed", # ローカルなので何でもOK
        service_id="local-llm",
    )
    
    # ★重要：ここで接続先を強制的に書き換える
    if ENDPOINT:
        service.client.base_url = ENDPOINT
    
    kernel.add_service(service)

    # 3. マニュアル読み込み
    manual_path = "secret_manual.md"
    manual_content = ""
    if os.path.exists(manual_path):
        with open(manual_path, "r", encoding="utf-8") as f:
            manual_content = f.read()
            print(f"✅ マニュアルを読み込みました: {manual_path}")
    else:
        print("❌ マニュアルが見つかりません。")
        return

    # ==========================================
    # 【ハンズオン2】プロンプトを作成する
    # ==========================================
    # TODO: 以下の変数をプロンプト内に埋め込む
    # マニュアルの中身: {{$manual}}
    # ユーザーの質問: {{$input}}
    
    prompt_template = """
    あなたは工場のベテラン管理アシスタントです。
    以下の【社内マニュアル】のみに基づいて回答してください。

    【社内マニュアル】
    (ここにマニュアル変数を書く)

    【ユーザーの質問】
    (ここにユーザー入力変数を書く)
    """

    # 4. 関数化
    kernel.add_function(
        prompt=prompt_template,
        function_name="ChatWithKnowledge",
        plugin_name="FactoryPlugin",
    )

    print("\n🤖 工場AIアシスタントが起動しました。（終了するには 'exit' と入力）")
    print("-" * 50)

    # 5. 実行ループ
    while True:
        user_input = input("あなた > ")
        if user_input.lower() in ["exit", "quit"]:
            break

        print("AI > 考え中...", end="", flush=True)
        
        try:
            arguments = KernelArguments(manual=manual_content, input=user_input)
            result = await kernel.invoke(
                function_name="ChatWithKnowledge",
                plugin_name="FactoryPlugin",
                arguments=arguments
            )
            print(f"\rAI > {result}\n")
            
        except Exception as e:
            print(f"\n❌ エラーが発生しました: {e}")

if __name__ == "__main__":
    asyncio.run(main())
```

**ファイル名**: `requirements.txt`

Plaintext

```
semantic-kernel>=1.0.0
``````

### 3. 【Bさん用】VB.NET雛形（穴埋め式）

Bさんは Visual Studio で「コンソールアプリ (.NET 8)」を新規作成し、NuGetで `Microsoft.SemanticKernel` をインストールした状態の `Program.vb` に以下を貼り付けてもらいます。

**ファイル名**: `Program.vb`

VB.Net

```
Imports Microsoft.SemanticKernel
Imports Microsoft.SemanticKernel.Connectors.OpenAI

Module Program
    Sub Main(args As String())
        MainAsync(args).GetAwaiter().GetResult()
    End Sub

    Async Function MainAsync(args As String()) As Task
        ' ==========================================
        ' 【ハンズオン1】ここを設定する
        ' ==========================================
        ' TODO: LM Studioのローカルサーバー情報を設定
        Dim endpoint As String = "" ' 例: http://localhost:1234/v1
        Dim modelId As String = "llama-3.1-8b-instruct"
        
        ' 1. カーネルの作成
        Dim builder = Kernel.CreateBuilder()

        ' TODO: ここで OpenAI ではなく「ローカルLLM」につなぐ設定を書く
        ' ヒント: .AddOpenAIChatCompletion(modelId, apiKey, endpoint) を使う
        If Not String.IsNullOrEmpty(endpoint) Then
             builder.AddOpenAIChatCompletion(
                modelId:=modelId,
                apiKey:="not-needed",
                endpoint:=New Uri(endpoint)
            )
        End If
        
        Dim kernel = builder.Build()

        ' 2. マニュアル読み込み
        Dim manualPath As String = "secret_manual.md"
        Dim manualContent As String = ""
        If System.IO.File.Exists(manualPath) Then
            manualContent = System.IO.File.ReadAllText(manualPath)
            Console.WriteLine($"✅ マニュアルを読み込みました: {manualPath}")
        Else
            Console.WriteLine("❌ マニュアルが見つかりません。")
            Return
        End If

        ' ==========================================
        ' 【ハンズオン2】プロンプトを作成する
        ' ==========================================
        ' TODO: {{$manual}} と {{$input}} を埋め込む
        Dim promptTemplate As String = "
        あなたは工場のベテラン管理アシスタントです。
        以下の【社内マニュアル】のみに基づいて回答してください。

        【社内マニュアル】
        (ここにマニュアル変数を書く)

        【ユーザーの質問】
        (ここにユーザー入力変数を書く)
        "

        ' 3. 関数化
        Dim chatFunction = kernel.CreateFunctionFromPrompt(promptTemplate)

        Console.WriteLine(vbCrLf & "🤖 工場AIアシスタントが起動しました。（終了するには 'exit' と入力）")
        Console.WriteLine(New String("-"c, 50))

        ' 4. 実行ループ
        While True
            Console.Write("あなた > ")
            Dim userInput = Console.ReadLine()
            If String.IsNullOrWhiteSpace(userInput) OrElse userInput.ToLower() = "exit" Then Exit While

            Console.Write("AI > 考え中...")

            Try
                Dim arguments = New KernelArguments()
                arguments("manual") = manualContent
                arguments("input") = userInput

                Dim result = Await kernel.InvokeAsync(chatFunction, arguments)
                Console.WriteLine(vbCr & "AI > " & result.ToString() & vbCrLf)

            Catch ex As Exception
                Console.WriteLine(vbCrLf & "❌ エラーが発生しました: " & ex.Message)
            End Try
        End While
    End Function
End Module
```

---

---

## 4. コマンドプロンプトで実行

ここがエンジニアっぽい作業です。 Windowsキーを押して「cmd」と入力し、**「コマンドプロンプト」** を開きます。

以下のコマンドを一行ずつコピー＆ペーストして実行してください。

DOS

```
REM 1. 作業フォルダに移動（場所は自分の環境に合わせてね）
cd Desktop\MyAI

REM 2. 仮想環境（AI専用の部屋）を作る
python -m venv venv

REM 3. 部屋に入る（Windows用のコマンド）
venv\Scripts\activate

REM 4. Semantic Kernelをインストール（ここだけ少し待つ）
pip install semantic-kernel

REM 5. いざ実行！
python main.py
```

---

## 5. 感動の瞬間

コマンドプロンプトに以下の文字が出れば成功です。

Plaintext

```
✅ 秘伝のタレを読み込みました: secret_manual.md
💻 社内専用AIが起動しました。（終了: exit）
あなた > 
```

ここで、以下の質問を投げかけてください。

> **「サーバーAで502エラーが出た。再起動していい？」**

LM Studioの画面が一瞬動き、コマンドプロンプトに：

> **「バカ野郎、再起動は厳禁だ！ まずDefenseOneを切れ！」**

と返ってくれば、**Windows上での完全ローカル・秘伝のタレAIの完成**です！

